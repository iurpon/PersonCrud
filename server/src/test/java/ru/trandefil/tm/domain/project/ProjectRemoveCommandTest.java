package ru.trandefil.tm.domain.project;

import org.junit.Test;
import ru.trandefil.tm.domain.AbstractCommandTest;

public class ProjectRemoveCommandTest  extends AbstractCommandTest {

    @Test
    public void execute() {
/*        projectService.delete(PROJECT1);
        Assert.assertEquals(projectService.getAll().size(),2);*/
    }
}